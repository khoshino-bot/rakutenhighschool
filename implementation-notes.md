# implementation-notes

## [2026-09-18 21:00] 見積体験システム 初版（index.html）

**実装形態**: 依存ゼロの単一HTML。要件4章の4条件（オフライン・タッチ完結・複数台配布・開発者不在運用）を最も単純に満たすため。

**仕様書にない判断**
- S1で端末（または「買わない」）未選択、S2でデータ量未選択のときは「つぎへ」を無効化。未選択のまま結果に行くと合計0円の見積になり体験が成立しないため。オプションは任意なので常に進める
- 割引トグルはプラン未選択でも適用し、合計は0円でクランプ（8章どおり）。プラン画面上で同時に選ぶ想定なので実害なし
- 下部バーはS0（スタート画面）では非表示。S1以降で常時表示
- 配色: 要件10章は楽天カラー可だが、社内デザインルール（ネイビー×ゴールド・ライトテーマ）を採用。楽天の名称を使わない方針と整合し、かつ管理ポリシー側の指示のため。色は `:root` のトークンに集約してあるので差し替えは容易
- 割引は3種（家族・22歳以下・65歳以上）。ペルソナA（20歳）・C（68歳・妻と二人）に対応する選択肢を用意するため（11章の突き合わせ）
- 端末画像は未提供のため SVG のシルエットを仮表示。`image` にパスを入れれば置き換わる

**マスタの値**: すべて仮値（投入日コメントあり）。第8事業部の公式価格で差し替え前提。楽天の正式プラン名は使っていない

**技術メモ**
- innerHTML で描画しているが、描画文字列はすべてファイル内固定の MASTER 由来で、利用者が文字を入力する欄は存在しない（NF-08・5.2）。Write ツールのセキュリティ hook が innerHTML で警告するため、ファイル書き込みは Bash 経由で行った
- 端末に何も保存しない（localStorage 等は未使用）。リセットは state を初期値で置き換えるだけ
- `dblclick` を preventDefault してタブレットのダブルタップ拡大を抑止。`viewport maximum-scale=1` も併用

**未検証**: 実機 iPad Safari。Mac の内蔵ブラウザで 1180×820 / 820×1180 / 375×812 のみ確認

## [2026-09-18 21:30] Codex 二重チェック反映・GitHub 公開

**Codex 指摘の反映（反映 5 件）**
- 画面遷移（はじめる／つぎへ／もどる／最初から）に 400ms の連打ガード。1回目の描画直後に同じ位置の別ボタンへ2回目が当たり画面を飛ばすのを防ぐ。選択操作は即時のまま
- 説明文（端末コメント・データ量の目安・割引条件・オプション説明・ヒント）を 14px → 16px（NF-04）
- ヘッダーに「最初から」を常設（S1〜S3）。途中離脱した体験者をスタッフが即初期化できるように（NF-10）
- 左右の safe-area（横向き iPad）に対応
- document 全体の dblclick 抑止を削除。タッチの二重 click 対策にならず、`touch-action: manipulation` で十分なため。`maximum-scale=1` はキオスク用途なので残す

**反映しなかった指摘と理由**
- 「実際の楽天プラン名を使うべき」: 依頼主から「プラン名はそのまま使えないので独自名に」と明示指示があるため独自名を維持
- 「未選択でも次へ進めるべき」: 未選択で結果に行くと 0 円の見積になり体験が成立しないため、必須選択を維持（要件書への追記は依頼側の宿題として報告）
- 「端末は買わない」を devices 配列に統合: 動作に影響なく、マスタ編集者が実機種と混同しないよう別扱いのままにした
- Safari のスワイプ戻る: ホーム画面追加（全画面）で開けば履歴が無く発生しない。運用手順で案内

**公開**: 個人アカウント khoshino-bot に公開リポジトリ `rakutenhighschool` を作成しプッシュ済み。GitHub Pages の有効化は権限で止まったため未実施（ユーザーが GitHub 上で Settings → Pages → Branch: main を選ぶ）。オフライン対応として `sw.js`（Service Worker）を追加。file:// 直開きでは登録せず、http(s) で開いたときだけ有効

## [2026-09-19] 店頭シミュレーター型レイアウトへ全面改修

**きっかけ**: ユーザーから「しょぼい。楽天モバイルショップの店頭シミュレーター（写真）のように、製品・支払回数・毎月のGBを選んで月額を出す形にしたい」。U-NEXT 付きプランは除外指示。

**判断**
- ウィザード（S0〜S4）を廃止し、料金プラン／製品／オプションの3カラム＋下段「月々のお支払いイメージ」の1画面構成に変更。要件書6.1のステップ遷移とは異なるが、ユーザーの明示指示を優先。下段は `position: sticky` で常時表示にして「全画面で月額合計を常時表示」は維持
- 配色を楽天系のクリムゾン（`--brand:#bf0000`）に変更。要件書10章が楽天ブランドカラーを許容しており、ユーザーが店頭の見え方を求めたため。社内デザインルール（ネイビー×ゴールド）からは外れる。色は `:root` のトークンに集約
- 申込区分（新規／MNP／プラン変更／機種変更）は店頭画面の再現として残したが金額には影響しない
- 製品はブランド（iPhone／Android）→ 機種の2段セレクト。先頭に「製品は買わない」。初期値は買わない
- 分割払いは 1〜(n-1)回目と n回目 を分けて表示（端数は最終回に寄せる）
- データ量未選択のときは月額を出さず「データ量を選ぶと月額が出ます」と表示。プラン欄は最小〜最大の範囲表示
- 割引に「こども割 −440円（仮）」を追加し4種に

**検証（内蔵ブラウザ 1180×820 / 820×1180）**: 20GB＋iPhone 17 48回＋保証＋22歳以下割 = 5,872円、一括時 3,168円＋初回 129,800円、24回 8,576円、48回の内訳 2,704円×47＋2,712円。リセットで全初期化。コンソールエラーなし。実機 iPad は未検証
